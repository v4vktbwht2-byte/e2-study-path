# START HERE — Codexへ一式丸投げする手順

このフォルダーは、**英語初学者から英検2級相当の力を目指す自己学習PWA**「E2 Study Path（仮称）」を、Codexに段階的かつ自律的に実装してもらうための引き継ぎ一式です。

## 最短の使い方

1. このフォルダーを任意の作業場所へ展開する。
2. フォルダーをGitリポジトリとして開く。未初期化なら `git init` を実行する。
3. Codexを**このフォルダー直下**で起動する。
4. `MASTER_PROMPT.md` の全文をCodexへ渡す。
5. Codexには、Phase 00からPhase 10まで自動継続させる。
6. 学習コンテンツ量を増やすときだけ、Phase 11のコンテンツ拡張プロンプトを追加で使う。

Codexは起動時に `AGENTS.md` を自動的に読み取る想定です。`AGENTS.md` は短い恒常ルール、`docs/` は詳細仕様、`prompts/` はフェーズごとの実装指示です。

## Codexへ最初に貼る文

`MASTER_PROMPT.md` をそのまま貼ってください。ファイル参照が使える環境では、次でも構いません。

```text
@MASTER_PROMPT.md を読み、記載されたPhase 00からPhase 10までを順番に実行してください。
```

## 実装を一度に任せる場合

Codexには次を許可してください。

- リポジトリ内ファイルの作成・変更
- npmパッケージのインストール
- lint、型検査、単体テスト、E2Eテスト、ビルドの実行
- GitHub Actionsやデプロイ設定ファイルの作成

外部サービスの認証情報、課金API、ドメイン設定は不要です。初期版はローカル保存・オフライン優先で完成します。

## フェーズ単位で進める場合

`prompts/` のファイルを番号順に1つずつCodexへ渡してください。各フェーズ終了時に、Codexは `docs/20_IMPLEMENTATION_STATUS.md` と `PLANS.md` を更新し、品質ゲートを実行します。

## 完成の定義

「画面が表示できた」だけでは完成ではありません。最低限、以下が必要です。

- 初回診断から推奨開始ステージを提示できる
- 初学者向けレッスンを進められる
- 単語集中モードで新規学習・復習・苦手練習ができる
- 復習予定が回答履歴に応じて変化する
- ブラウザーを閉じても学習履歴が残る
- オフライン再起動できる
- バックアップと復元ができる
- スマートフォンで無理なく操作できる
- lint、型検査、テスト、ビルドが成功する
- 公式過去問・公式音源を転載していない

詳しい受け入れ条件は `docs/17_ACCEPTANCE_CRITERIA_TRACEABILITY.md` を参照してください。

## 重要な方針

- アプリ名は仮称。公開前に変更可能。
- 英検公式アプリではないことを明示する。
- 公式ロゴ、公式問題、公式音声をアプリ内へ複製しない。
- 初期版はAI APIなしで完成させる。
- 教材はすべてオリジナルまたは利用許諾が明確なものにする。
- 「エビングハウスの忘却曲線」は固定の忘却率を断定するのではなく、**初期は短い間隔、その後は回答履歴に応じて間隔を広げる設計思想**として使う。

## Windowsでの推奨開始例

PowerShell:

```powershell
cd C:\work\e2-study-path
 git init
 codex
```

WSLを使う場合:

```bash
cd ~/work/e2-study-path
git init
codex
```

Node.jsとnpmは、実装時点でViteが正式対応する安定版をCodexに確認させ、`.nvmrc` と `package.json` に記録させます。
